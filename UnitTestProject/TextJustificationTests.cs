using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections.Generic;
using System.Text;

namespace UnitTestProject
{
    [TestClass]
    public class TextJustificationTests
    {
        [TestMethod]
        public void FullJustify()
        {
            // TODO:  implement Tests
            //string[] words = { "This", "is", "an", "example", "of", "text", "justification." };
            //int L = 16;
            //IList<string> s = l.FullJustify(words, L);// 

            // words = new string[] { "This", "is", "an", "example", "of", "text", "justification." };
            // L = 15;
            // s = l.FullJustify(words, L);// 



            //words = new string[] { "What", "must", "be", "shall", "be." };
            //L = 12;
            //s = l.FullJustify(words, L);


            //words = new string[] {
            //    "Listen", "to", "many,", "speak",
            //    "to", "a",
            //    "few." };
            //L = 6;
            // s = l.FullJustify(words, L);
            ////IList<string> s= l.FullJustify(words,L);


            ////for (int i = 0; i < 100000; i++)
            ////{

            //words = new string[] { "ThisThisThisThis", "is", "an", "example", "of", "text", "justification." };
            //L = 16;
            //s = l.FullJustify(words, L);


            //words = new string[] { };
            //L = 2;
            //s = l.FullJustify(words, L);
            ////IList<string> s = l.FullJustify(words, L);// 2 spaces


            //words = new string[] {"ts","df", "ffff", "12", "dddd" };
            //L = 4;
            //s = l.FullJustify(words, L);
            ////IList<string> s = l.FullJustify(words, L);// 2 spaces

            ////}

        }
    }
}
